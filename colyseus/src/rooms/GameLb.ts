export function updateTeamScore(teamId: string){
    //insert redis logic to update a teams score securely
}

export function getTeamScore(teamId: string): number{
    //insert redis logic to get a single teams score
    return 0;
}

export function getAllTeamScores(): Array<any>{
    //inert redis logic to get all teams scores
    return [];
}